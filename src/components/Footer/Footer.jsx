import React from 'react'

import './Footer.scss'

export default function Footer() {
    return (
        <footer className="footer animation-up">
            <address className="d-flex align-items-center justify-content-center">
                Desenvolvido por @<span>luca</span>.<span>merighi</span>
            </address>
        </footer>
    )
}